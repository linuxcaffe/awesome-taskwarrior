# wrapper-template.meta - Template for wrapper/script .meta files
# Version: 2.0.0
#
# This file describes a wrapper/script application for awesome-taskwarrior.
# Use this template for executables that go in ~/.task/scripts/

# Required: Application identifier (used in commands like `tw.py install mywrapper`)
# Rules: lowercase letters, numbers, hyphens, underscores only
name=mywrapper

# Required: Semantic version number
version=1.0.0

# Required: One-line description (shown in listings, keep under 80 chars)
description=Brief description of what your wrapper script does

# Optional: Comma-separated list of requirements
# Note: Wrapper scripts often have language-specific requirements
requires=bash>=4.0,taskwarrior>=2.6.0

# Required: Comma-separated list of filename:type pairs
# Types: script, config, doc (hooks not applicable for wrappers)
# Order matters - must match order of checksums field
# README.md will be renamed to appname_README.md when installed
files=mywrapper:script,mywrapper.rc:config,README.md:doc

# Required: Base URL for curl downloads
# Must serve raw file content (not HTML)
base_url=https://raw.githubusercontent.com/username/mywrapper/main/

# Optional: SHA256 checksums for file integrity verification
# Format: sha256:hash1,sha256:hash2,sha256:hash3,...
# Order must match files= field
# checksums=sha256:abc123...,sha256:def456...,sha256:789xyz...
checksums=
