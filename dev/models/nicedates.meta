# nicedates.meta
#
# Real-world example: Human-readable date parsing wrapper
# Demonstrates a simple but effective wrapper application

name=nicedates
short_desc=Human-readable date parsing and formatting for Taskwarrior
version=2.1.0
repo=https://github.com/linuxcaffe/tw-nicedates-hook
type=wrapper
install_script=nicedates.install
author=linuxcaffe
wrapper=yes

long_desc=Enhances Taskwarrior's date parsing with natural language support and
  improved date formatting. Allows expressions like 'tomorrow', 'next week',
  'in 3 days', '2 weeks from now', and many other human-friendly formats.
  Transparently wraps task commands, converting natural language dates to
  Taskwarrior's expected format. Also improves date output formatting in task
  lists and reports. Works seamlessly in wrapper chains with other tools.

requires=python3>=3.6,taskwarrior>=2.6.2,python3-dateutil

conflicts=

provides=nicedates.py,nicedates

checksum_type=sha256
checksum=

tags=wrapper,dates,natural-language,parsing,formatting

# Installation notes:
# - Pure wrapper, no hooks or UDAs
# - Requires python-dateutil for advanced date parsing
# - Can be used standalone or in wrapper chain
# - Transparent pass-through for non-date commands
# - Configurable date format preferences
