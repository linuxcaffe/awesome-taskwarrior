# hook-template.meta - Template for .meta files
# Version: 2.0.0
#
# This file describes an application for the awesome-taskwarrior registry.
# Update all fields for your specific application.

# Required: Application identifier (used in commands like `tw.py install myapp`)
# Rules: lowercase letters, numbers, hyphens, underscores only
name=myapp

# Required: Semantic version number
version=1.0.0

# Required: One-line description (shown in listings, keep under 80 chars)
description=Brief description of what your hook application does

# Optional: Comma-separated list of requirements
# Formats: command, command>=version, command==version
requires=python3>=3.6,taskwarrior>=2.6.0

# Required: Comma-separated list of filename:type pairs
# Types: hook, script, config, doc
# Order matters - must match order of checksums field
# README.md will be renamed to appname_README.md when installed
files=on-add_myapp.py:hook,on-modify_myapp.py:hook,myapp.rc:config,README.md:doc

# Required: Base URL for curl downloads
# Must serve raw file content (not HTML)
# Usually points to GitHub raw, GitLab raw, etc.
base_url=https://raw.githubusercontent.com/username/myapp/main/

# Optional: SHA256 checksums for file integrity verification
# Format: sha256:hash1,sha256:hash2,sha256:hash3,...
# Order must match files= field
# Generate with: sha256sum file1 file2 file3 ...
# Leave empty or comment out if not using checksums
# checksums=sha256:abc123...,sha256:def456...,sha256:789xyz...,sha256:012uvw...
checksums=
